spring.application.name=task-service

spring.datasource.url=jdbc:mysql://localhost:3307/task_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}

#Production mode
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always
# never

auth-service.base-url=http://localhost:8081


server.port=8082

spring.profiles.active=local

auth.internal-key=${AUTH_INTERNAL_KEY}

spring.security.user.name=diana
spring.security.user.password=password

spring.jackson.default-property-inclusion=non_null

task.security.allowed-origins=http://localhost:4200